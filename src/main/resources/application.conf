server {
  host = "0.0.0.0"
  port = 8080
}

application {
  name = "ai-orders"
  version = "0.1.0-SNAPSHOT"
}

database {
  url = "jdbc:postgresql://localhost:5432/aiorders"
  username = "aiorders"
  password = "password"
  driver = "org.postgresql.Driver"
}